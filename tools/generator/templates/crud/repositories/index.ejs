import { EntityTarget } from "typeorm/common/EntityTarget";
import CrudRepository from "../_crud";
import I<%=templateName%>Repository from "./interface";
import <%=templateName%> from "../../models/<%=pathname%>";


export default class <%=templateName%>Repository extends CrudRepository implements I<%=templateName%>Repository {
    constructor(model: EntityTarget<<%=templateName%>>) {
        super(model, []);
        this.model = model;
        this.modelInstantiator = () => {return new <%=templateName%>};
    }
}